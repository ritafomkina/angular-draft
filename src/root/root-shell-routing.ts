import { Routes } from '@angular/router';
import { StartComponent } from './start/feature/start-demo/start-demo.component';

export const routes: Routes = [
  {
    path: 'start', component: StartComponent,
  },
];
